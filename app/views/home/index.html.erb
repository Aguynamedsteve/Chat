<br/>
<div class="jumbotron">
  <% if user_signed_in? %>
    <h1>Welcome back</h1>
      <p class="lead">
        <h2>The world's greatest real-time multilingual chat app in the history of the universe.</h2>
      </p>
        <div class="row-fluid">
          <div class="span6">
            <%= link_to "Chat now", messages_path, :class => 'btn btn-large btn-primary' %>
            <%= link_to "Sign Out", destroy_user_session_path, :method => :delete, :class => 'btn btn-large btn-danger' %>
          </div>
        </div>
  <% else %>
    <h1>Welcome</h1>
      <p class="lead">
        <h2>The world's greatest real-time multilingual chat app in the history of the universe.</h2>
      </p>
        <div class="row-fluid">
          <div class="span6">
            <%= link_to "Sign In", new_user_session_path, :class => "btn btn-primary" %>
            <%= link_to "Sign Up", new_user_registration_path, :class => "btn btn-primary" %>
          </div>
        </div>
  <% end %>
</div>

